import { LogEntryTypeComment } from './log-entry-type-comment';

describe('LogEntryTypeComment', () => {
  it('should create an instance', () => {
    expect(new LogEntryTypeComment()).toBeTruthy();
  });
});
